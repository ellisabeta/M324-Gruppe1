server:
  port: 8082

spring:
  application:
    name: flug-service
  data:
    mongodb:
      uri: ${MONGODB_FLIGHT_URI:mongodb://localhost:27017/flug_db}

services:
  airport:
    url: ${AIRPORT_SERVICE_URL:http://localhost:8081}
